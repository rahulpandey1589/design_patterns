using design_patterns.AbstractFactory.Factories;
using System;

namespace design_patterns.AbstractFactory
{
    /*
    public class AbstractFactoryClientApplication
    {
        static void Main(string[] args)
        {
            GuiFactory factory = new WindowGUIFactory();
            var btnObj = factory.CreateButton();
            var chkBoxObj = factory.CreateCheckbox();
            
            Console.WriteLine(btnObj.GetButtonType());
            Console.WriteLine(chkBoxObj.GetCheckboxType());

            Console.Read();
        }
    }
    */
}
